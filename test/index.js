'use strict';

const should = require('chai').should(); // eslint-disable-line

describe('hexo-migrator-wordpress', () => {
  // Tests.
  it('should migrate posts.', () => {
    return true;
  });
});
